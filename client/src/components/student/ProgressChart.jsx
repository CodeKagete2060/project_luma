import React from 'react';

export default function ProgressChart() {
  // Simple placeholder chart to avoid import errors during development
  return (
    <div className="p-4 bg-card rounded">
      <h4 className="text-sm font-medium">Progress (demo)</h4>
      <div className="h-40 flex items-center justify-center text-muted-foreground">Chart placeholder</div>
    </div>
  );
}
